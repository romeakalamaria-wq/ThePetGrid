# Install Phase 3.1 — Living Cells

Copy the project contents into the current ThePetGrid repository and replace matching files.

Stage only these files:

```powershell
git add "pages/world-experience.html" "css/world-experience.css" "js/world-experience.js" "CHANGELOG_WORLD_EXPERIENCE_PHASE3_1_LIVING_CELLS.md" "ATLAS_PHASE3_1_INSTALL.md"
git commit -m "World Experience Phase 3.1 Living Cells"
git push origin main
```

After Vercel deploy, open `/pages/world-experience.html` and hard-refresh with Ctrl+Shift+R.

(() => {
  "use strict";

  // Add your free Geoapify API key here, or enter it directly on the Map page.
  // Protect browser keys from the Geoapify dashboard with allowed HTTP referrers.
  window.ThePetGridGeoapify = Object.freeze({
    apiKey: "",
    language: "en",
    resultLimit: 150
  });
})();
